// The page itself lives with the rest of the Artifacts feature; pages/ stays a
// thin routing surface. Re-exported here so the route and existing imports do
// not need to know where the feature moved.
export { ArtifactsPage } from "../artifacts/ArtifactsPage";

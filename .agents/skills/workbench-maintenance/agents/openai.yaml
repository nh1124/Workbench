interface:
  display_name: "Workbench Maintenance"
  short_description: "Maintain Workbench knowledge freshness via Analyser proposals"
  default_prompt: "Use $workbench-maintenance to analyse recent observations, inspect affected knowledge, and record findings as Analyser proposals without promoting anything."
policy:
  allow_implicit_invocation: true
